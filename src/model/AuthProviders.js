class AuthProviders {
    constructor(providerId, name,createdDate) {

        this.providerId = providerId,
        this.providerName = name,
        this.createdDate = createdDate
    }
}
module.exports = AuthProviders;